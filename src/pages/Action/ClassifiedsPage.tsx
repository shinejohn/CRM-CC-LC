import React from 'react';
import { ComingSoon } from '@/components/ComingSoon';

export const ClassifiedsPage: React.FC = () => {
  return (
    <ComingSoon
      title="Classifieds"
      description="Post and browse classified advertisements."
      backPath="/"
    />
  );
};
